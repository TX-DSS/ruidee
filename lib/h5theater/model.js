// model
class Model {
    constructor(options) {
        // width
        this._w = options.width;
        // length
        this._l = options.length;
        // height
        this._h = options.height;
        // position {x, y, z}
        this._pos = options.position;
        // category svg|img
        this._cat = options.category;
        // model data
        this.data = options.data;
    }

    show() {

    }

    hide() {

    }

    move() {

    }
}
